apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fear-nightfall-data
  namespace: apps
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 32Gi
